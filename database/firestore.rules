rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /photobooth_uploads/{document} {
      allow read, create: if true;
      allow update, delete: if false; // Only allow creation and reading of photobooth uploads
    }
  }
}
